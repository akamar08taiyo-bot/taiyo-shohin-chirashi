# 消耗品チラシ作成（太陽シルバーサービス㈱）

病院・施設向けの業務用消耗品カタログチラシです。ビルド不要の素のHTML＋バニラJSで、GitHub Pagesでそのまま配信します。

公開先: https://akamar08taiyo-bot.github.io/taiyo-shohin-chirashi/

## 構成

```
index.html          メインメニュー（チラシ / 見積 / 表紙 / 価格表 と 発行営業所の選択）
flyers.html          チラシ一覧（メーカーから探す / 使用用途から探す）
quote.html            見積・提案書作成（商品を選ぶ → 御見積書を印刷）
cover.html            見積書カバー（表題・お客様名を入れてA4横で印刷）
custom.html           自分で選んでチラシを作る（用途／メーカー・ジャンル→商品4点→A4縦で印刷）
kao-rocket.html      花王・ロケット石鹸（全11ページ・44商品）
japacks.html         ジャパックス（全8ページ・32商品）
paper.html            紙製品（全3ページ・12商品。他3商品は商品編集ピッカーから選択可）
taketora.html         竹虎（医療・介護用ディスポ用品、全5ページ・20商品。他13商品はピッカーから選択可）
lion.html             ライオンハイジーン（全36ページ・固定チラシ144商品）
omutsu.html           紙おむつ・尿とりパッド（全29ページ・116商品）
saraya.html           サラヤ（全4ページ・固定チラシ16商品。他2商品はピッカー専用）
care.html             おしりふき・とろみ剤（全2ページ・8商品。他1商品はピッカー専用）
price-calc.html       価格・単価計算シート（社内用）
validate.cjs          データ整合性チェック（`node validate.cjs`）
assets/
  design.css          デザイントークン・A4ページのCSS（794×1123px = A4 @96dpi）
  flyer-render.js      data/ からページを組み立てる共通レンダラー
  pricing.js           単価計算ロジック（localStorage 共有）
  offices.js           全19営業所マスタと選択状態の保存
data/
  products.json         商品マスター（412商品。重複を除去し、確認済みJAN・規格・入数を収録）
  pages.json            ページ構成（見出し・リード文・サイズ展開欄・注記・写真枠の高さ）
  price-rows.json       価格計算シート用の行データ（全商品の確認済み容量・入数を収録。価格自体は初期状態では非公開）
images/
  kao/ rocket/ japacks/ daio/ shinbashi/ showa/ lion/   商品写真（メーカー公式画像・公式カタログ掲載画像）
  taketora/                            商品写真（メーカー提供画像。背景トリミングのみ、色は加工していない）
```

## 変更したら必ず実行する

```bash
node validate.cjs
node audit-images.cjs --compact
```

商品を追加・差し替えたあとの確認用。以下を機械的に検証する。

- `products.json` の `id` が重複していないか
- `price-rows.json` と 1:1 で対応しているか
- JAN-13のチェックデジットが正しいか、未確認情報や空の入数が残っていないか
- 画像ファイルが実在するか
- **1ページ4商品ちょうどか**（過不足があるとカードが潰れ、`overflow:hidden` で文字が見えなくなる）

`pages.json` に無いページ名を指定した商品、または自動改ページ商品で `fixedFlyer: false` を指定した商品は「ピッカー専用」として扱われる。固定チラシには印刷されないが、商品編集ピッカー・自作チラシ・見積・価格表では利用できる。現在20件（紙製品3件、竹虎13件、ライオン1件、サラヤ2件、ケア1件）。サラヤの2件は消耗品ではない設置用スタンド／ディスペンサーで、18商品のうち16商品を4ページに固定するためピッカー専用とした。**予備商品のページ番号を連番の誤りとみなして直すと、1ページ5商品になり崩れるため変更しない。**

データとテンプレートを分離しているため、**HTMLを増やさず** `data/*.json` の編集だけで内容を更新できます。

## 商品・ページの追加や修正

- 商品を追加・修正する: `data/products.json` に1件追加（`flier` と `page` は既存の値と完全一致させる）。画像は `images/<メーカー>/` に配置し `image` に相対パスを書く。
- ページ（見出し・リード文・サイズ展開欄・注記）を追加・修正する: `data/pages.json` の該当 `pages[]` を編集。
- 1ページに4商品ちょうど（ルール）。過不足があると空欄またはグリッドが崩れるので確認する。
- 価格を入力する: `price-calc.html` で仕入価格を入力すると、利益率20%（変更可）を基準に販売金額を自動計算する。利益率または金額を直接編集すればもう片方を逆算する。単価の基準（「100mLあたり」の「100」「mL」部分）も数値・単位ともに修正できる。ブラウザの `localStorage`（キー `tsss-price-v1`）に保存され、各チラシページの「価格表示（社内用）」をONにするとその場に反映される。他の端末には自動同期されない。
- お客様チェック欄: 各チラシの「お客様チェック欄」をONにすると、商品カードの右上に空欄の四角が出る。配布したチラシにお客様が印を付けてそのまま見積依頼に使える。社内用の「見積に追加」と違い**印刷にも出る**。
- 見積を施設ごとに保存する: `quote.html` の見積書タブ下部で施設名を付けて保存。商品と入力内容がまとめて残り、あとから呼び出して続きを作れる（`localStorage` キー `tss_quote_saved_v1`）。同じ施設名なら上書き。
- 自分でチラシを組む: `custom.html`。用途（16分類）またはメーカーを選ぶと商品が並び、メーカー選択時は「テープ止め／パンツタイプ／日中用パッド／夜用パッド」などの商品ジャンル見出し・絞り込みが出る。**4点ちょうど**チェックするとA4チラシができ、5点目は選べない。各印刷ページには必ず4商品と社名・営業所情報を入れ、画像未読込や文字切れを検知した場合は印刷を中止する。
- 掲載商品を差し替える: 各チラシページの「商品を編集」をONにすると、カードごとに「商品を変更」ボタンが出る。押すと全412商品から検索（商品名・メーカー）／場所（ページ分類）／メーカーで絞り込んで選べる。差し替えは `localStorage`（キー `tss_chirashi_composition_v1`）にページ単位で保存され、ページの「おすすめ構成に戻す」で元の構成に戻せる。印刷・PDF保存時は編集用のボタンは表示されない。

## ローカル確認

ビルド不要。ただし `fetch()` で `data/*.json` を読むため、`file://` では動かない。簡易サーバーを立てて確認する。

```bash
npx serve .
# または
python -m http.server 8080
```

## デプロイ

```bash
rtk git add -A
rtk git commit -m "内容の要約"
rtk git push
```

main ブランチ直下を GitHub Pages で配信する設定。反映は push 後1〜3分。確認は Ctrl+F5（スーパーリロード）で。

## 由来・データの出典

`design_handoff_flier`（Claude Design由来の引き継ぎ資料）から機械抽出・移植しました。デザイン（配色・寸法・書体）は元の `.dc.html` に忠実です。掲載ルール（1ページ4商品、写真高さ統一、公式画像のみ使用、不明情報は「未確認」と明記）は踏襲しています。

2026-09-02時点で、掲載商品のJAN・品番・容量・入数をメーカー公式商品ページまたは公式カタログと再照合済みです。確認できない数値を推測で補完しない方針は継続し、検証処理で「未確認」や空の入数が再混入しないようにしています。

## 価格表の並び順

`data/price-rows.json` の**配列順がそのまま価格表の表示順**になる。
メーカーごとにまとめ、メーカー内はメーカー価格一覧（Excel）の掲載順に合わせてある
（`xlsOrder` が並び順の元。Excelに無い商品はメーカー内の末尾）。
価格改定のとき、Excelと画面を上から突き合わせながら入力できるようにするため。

メーカーの表示順は `price-rows.json` の並びで決まる。変更するときは
並べ替えスクリプトの `MAKER_ORDER` と同じ考え方で配列を並べ替える。

## 写真と商品の照合について

全412商品の画像ファイルの実在、実表示時の拡大率、過剰な余白、同一画像ハッシュを `audit-images.cjs` で監査する。公式ページ・公式カタログで高解像度画像を確認できた商品は置換し、確認できない画像を生成・推測で作り直さない。確認方法は次のとおり。

1. **ファイル名のJANとデータのJANの一致**（44商品が該当）→ 全件一致
2. **一覧シートでの目視確認** → 規格の数値（2.5kg・4.5L・45L・50枚入など）が
   パッケージの表示と一致することを確認

同じ写真・同じ仕様で二重登録されていた `エルヴェール 170m 6ロール`（旧id76）と `薬用消毒できるアルコールタオル つめかえ用`（旧id90）は、2026-08-30に重複側を削除しました。

## 保留中の確認事項

- ~~**トイレットペーパー「200m芯なし」品**~~ → **解決済み**。id110として掲載し、JAN 4902011722734・200m×6ロールを公式ページで照合済みです。
- ~~**竹虎商品のJAN・サイズ・入数**~~ → **解決済み（2026-08-30）**。食事用エプロン・ガウン等10商品も公式商品ページで照合し、色・サイズ違いは複数の品番/JANを規格欄へ明記しました。
- **フェルラック®のサイズ展開**: No.10〜No.100 の10点は、代表商品「フェルラック® エプロン」がチラシに載っているため、ピッカー専用の予備にしています（1ページ4商品・代表商品を載せるという掲載ルールに従うため）。見積では通常どおり選べます。
- **竹虎の手袋**: 現在掲載中の `タケトラ® ニトリル手袋` `プラスチック手袋100/200` は公式商品ページで品番・JAN・サイズ・入数を照合済みです。
