# Learnings（このリポジトリで得た知見）

セッションをまたいで再利用する知見を記録する。着手前にこのファイルと `README.md` を読むこと。

---

## 1. 「タイポに見えるデータ」を直す前に、設計意図を確認する

**失敗（2026-08-27）**: `products.json` に `"page": "04 手指衛生・清拭"` という項目があった。
`pages.json` の紙製品チラシは 01〜03 しかないため「連番の誤り」と判断して `03` に修正した。

結果、そのページが **5商品** になり、カード高さが 322px → 216px に潰れ、
`.tss-card { overflow: hidden }` によって価格行が **80px ぶん不可視で切り取られた**。
画面上は何のエラーも出ないため、気づきにくい壊れ方だった。

**正しい理解**: `pages.json` に存在しないページ名は「ピッカー専用の予備商品」を意味する仕様。
チラシには印刷されず、「商品を差し替え」ピッカーからのみ選べる。README に
「全3ページ・12商品。他4商品は商品編集ピッカーから選択可」と明記されていた。

**教訓**:
- データの不整合を見つけたら、直す前に `README.md` と実際の描画結果を確認する。
- 「1ページ4商品ちょうど」のような不変条件は、目視ではなく `node validate.cjs` で検証する。
- 単独で見ると誤りに見える値でも、別ファイルの仕組みと組み合わさって機能していることがある。

---

## 2. `object-fit: contain` では、画像ファイルの余白を削っても表示サイズは変わらない

**検証（2026-08-27）**: 「商品写真が小さい」という指摘に対し、
まず画像ファイル内の白余白をトリミングしようとした。しかし実測の結果、**効果がない**と判明した。

理由: `object-fit: contain` は「ファイル全体」を枠に収める。
高さで制約されている画像は、左右の余白を削っても高さの制約が変わらないため、描画サイズは同一になる。

例）350×350 の画像（中身は幅53%・高さ100%）を 351×140 の枠に入れる:
- トリミング前: 高さ制約 → 124×124 で描画、中身は 65×124
- トリミング後（185×350）: 高さ制約 → 124 の高さで描画、中身は 65×124 ← **同じ**

**効くのは枠の高さ（`pages.json` の `photoHeight`）だけ**。
画像加工は不要で、リスク（写真を壊す）だけが残る。

**教訓**: レイアウトの数値は推測せず、ブラウザで実測してから手を打つ。

---

## 3. 固定高さの写真枠は、文字を不可視で切り取る事故を起こす

**もとの実装**: `.tss-card-photo { flex: none; height: <token>px }`
→ 本文が増える（商品名が長い・価格表示ON・Webフォント未ロード）と、
本文が card からはみ出し `overflow: hidden` で **黙って消える**。

**対策（採用済み）**:
```css
.tss-card-photo { flex: 0 1 auto; min-height: 0; }  /* 足りなければ写真が縮む */
.tss-card-body  { flex: 1 0 auto; }                 /* 本文は自分の高さを主張し、縮まない */
```
これで不足分を写真枠が吸収する。文字が消える代わりに写真が少し小さくなるだけで済む。

**副次効果**: Google Fonts が読めない社内ネットワークでフォールバックしても、
実測で文字欠け・はみ出しゼロを確認できた（対策前は崩れる構造だった）。

---

## 4. JSON を書き換えるときはインデント幅を保つ

`JSON.stringify(data, null, 2)` で `products.json` を書き戻したところ、
元が **1スペース** インデントだったため全1412行が差分になった。
レビュー不能な差分になり、本来の1行の変更が埋もれる。

**対策**: 1行だけ直すなら Edit ツールで部分置換する。
全体を書き戻すなら元のインデント幅（このリポジトリは `data/*.json` が1スペース、
`pages.json` は2スペース）を確認して合わせる。
`git diff --ignore-cr-at-eol` で本当の差分量を確認してからコミットする。

---

## 5. 色は「印刷して配る」前提で決める

チラシは紙で配布するため、画面で薄く見える色は紙でさらに読めなくなる。
白背景に対するコントラスト比を実測し、WCAG AA（4.5:1）を満たすよう調整した。

| 用途 | 変更前 | 比 | 変更後 | 比 |
|---|---|---|---|---|
| JANコード | `#8aa0a2` | 2.75:1 | `#5c7376` | 5.04:1 |
| 注記 | `#6f8688` | 3.86:1 | `#5c7376` | 5.04:1 |
| 価格 | `#c25a17` | 4.40:1 | `#b85214` | 4.94:1 |

また `@media print` に `print-color-adjust: exact` が無く、
ブラウザの「背景のグラフィック」設定次第で見出しの帯やタグの色が飛ぶ状態だった。追加済み。

---

## 6. 検証は目視ではなくブラウザで実測する

このリポジトリの不具合は「見た目は正常だが情報が欠けている」型が多い。
以下を `javascript_tool` で実測するのが確実。

```js
// カード内で文字が切り取られていないか（最重要）
document.querySelectorAll('.tss-card-body').forEach(b => b.scrollHeight > b.clientHeight)
// A4からはみ出していないか
[...document.querySelectorAll('.page')].some(p => p.scrollHeight > p.clientHeight)
// 写真枠がページ内で揃っているか
[...pg.querySelectorAll('.tss-card-photo')].map(p => p.getBoundingClientRect().height)
```

**最悪ケースで測ること**: 価格表示ONは本文が1行増えるため、
必ず ON の状態でも検証する。OFF だけで確認すると本番で切れる。

注意点:
- 画像は `loading="lazy"` のため、画面外だと `naturalWidth === 0`。
  実測前に `loading='eager'` にして `onload` を待つ。
- テストで `localStorage` に価格やカートを入れたら、**必ず `localStorage.clear()` で後始末する**。
  残すと次の確認で誤った結果を見ることになる。

---

## 7. 画面遷移は階層どおりに戻す

`index.html`（メインメニュー）→ `flyers.html`（チラシ一覧）→ 各チラシ、という階層。
チラシページの戻るリンクが `index.html` を指していて1段飛ばしていた。
ラベルも「← 一覧へ」「← メインメニューへ」が混在していた。

現在の正しい状態:

| ページ | 戻り先 | ラベル |
|---|---|---|
| flyers / quote / price-calc | `index.html` | ← メインメニューへ |
| 各チラシ（4種） | `flyers.html` | ← チラシ一覧 |

---

## 8. データは必ず一次情報で裏を取る

「sasaYell」というメーカー名が入っていたが、実際は **ブランド名** で、
メーカーは **昭和紙工株式会社** だった。
公式サイトで確認したところ、正式名は `SaSaYell`（大文字S・Y）、JAN は `4957434009139` と判明。

同時に、説明文にあった「約200×200mmの大判。水99%・無香料・ノンアルコール。」は
**公式ページに記載がない**ことが分かったため、記載のある事実だけに書き換えた。

**教訓**: 商品スペックは推測で書かない。裏が取れないものは「未確認」のままにする。
ユーザーからも「わからない場合は絶対に入れない」と明確な指示がある。

---

## 9. チャットに貼られた画像はファイルとして保存できない

ユーザーが会話に画像を貼っても、こちらにはそれをディスクへ書き出す手段がない
（Write はテキストのみ、Read は既存ファイルの読み取りのみ）。

**対応**: 保存先フォルダを具体的に伝えて保存してもらう。
そのうえで `Get-ChildItem` / `find` で実ファイルを探す。
なお OneDrive 配下はフォルダが移動されることがあるため、
名前で全体検索するほうが確実（例: `Get-ChildItem -Recurse -Directory | Where-Object Name -like "*竹虎*"`）。

Windows の日本語パスは Node の `fs` で ENOENT になることがある。
PowerShell の `-LiteralPath` を使うと確実に扱える。

---

## 10. PDFカタログからの仕様抽出は座標を使う

メーカーのカタログPDFは `get_text()` の素の抽出だと表の行が崩れ、
商品番号と規格の対応が入れ替わる。PyMuPDF が使えるので、
`get_text("words")` で座標を取り、y座標でグループ化して行を復元する。

```python
words = doc[p].get_text("words")   # x0,y0,x1,y1,word,...
words.sort(key=lambda w: (round(w[1]/4.0), w[0]))
```

WebFetch で PDF を渡すと「バイナリで読めない」と返るが、
**ローカルにダウンロードはされている**ので、そのパスを PyMuPDF で開けばよい。

---

## 11. 印刷帳票は「最悪ケースの件数」で高さを実測する

見積書は商品1件では収まっても、メーカーが3つに分かれると
グループ見出し＋表ヘッダー＋小計行が3組ぶん増えてA4を超える。
`.doc` の `min-height: 297mm` があるため画面上は破綻して見えず、
**合計欄だけが静かに2ページ目へ送られる**。

検証は「複数メーカーにまたがる状態」で `.doc` の実高さを測り、
1123px（A4@96dpi）以下かを確認する。

---

## 12. ブラウザペインが非表示だと測定値が壊れる

`window.innerWidth` が 0 になり、`getBoundingClientRect()` が
すべて 0 付近を返すため「横スクロールあり」などの誤検知が出る。
測定前に `resize_window` で明示的にビューポートを与えること。

また、`fetch` でデータを読んでから描画するページは、
`navigate` 直後に要素を探すと**空振りする**。
要素が現れるまで待ってから操作する。

```js
for (let i=0; i<40 && document.querySelectorAll(SEL).length===0; i++)
  await new Promise(r=>setTimeout(r,100));
```

---

## 13. 太陽ポータルへの登録

このアプリは業務アプリポータル（`taiyo-portal`）のタイルから開く。
- リポジトリ: `C:\Users\akama\taiyo-portal`（**ブランチは `master`**。`main` ではない）
- `index.html` 内の `APPS` 配列に1件追加するだけ
- 色クラスは `c-teal` `c-cyan` `c-orange` など既定のものから選ぶ（新規追加は不要）
- 登録名は「チラシ作成（消耗品）」。レンタル用の別アプリ（`taiyo-chirashi`）と
  区別するため、そちらは「チラシ作成（レンタル）」に改称済み

---

## 14. 「反映されない」の原因はキャッシュ用の `?v=` の上げ忘れ

データもCSSも `?v=YYYYMMDD-n` を付けて配信している。
**データを増やしたのに `?v=` を据え置くと、既存利用者のブラウザは
古い `products.json` を読み続ける**。GitHub Pages 側は更新済みなので、
「サーバは正しいのに利用者だけ古い」という一番わかりにくい形になる。

商品・ページ・CSS を触ったら、必ず全HTMLとJSの `?v=` を上げる。

```bash
node -e "const fs=require('fs');fs.readdirSync('.').filter(f=>f.endsWith('.html')).concat(['assets/flyer-render.js']).forEach(f=>{const s=fs.readFileSync(f,'utf8');const t=s.split('v=旧').join('v=新');if(t!==s)fs.writeFileSync(f,t)})"
```

---

## 15. 写真の「大きさが不揃い」は2つの原因が重なる

見た目が揃わないとき、原因は必ず次のどちらか（または両方）。

1. **画像内の余白量がバラバラ** — `object-fit: contain` は画像全体を枠に
   収めるので、余白が多い画像ほど商品が小さく見える。
   → 非白画素の外接矩形で切り詰めて揃える（占有率 0.41〜1.00 → ほぼ1.00）。
2. **元画像が枠より小さい** — `max-width/max-height: 100%` だけだと
   拡大されず、元のピクセルサイズのまま表示される。
   → `width: 100%; height: 100%; object-fit: contain;` にして必ず枠に合わせる。

検証は「表示高さの最小と最大」を測る。揃っていれば差は1px程度になる。

```js
[...document.querySelectorAll('.tss-card-photo img')].map(i => i.getBoundingClientRect().height)
```

メーカー公開画像は小さいことがある（カミ商事は 59×140px）。
拡大すると粗くなるが、**大きさの不揃いの方が印象を損なう**ので揃えを優先し、
粗さは「メーカー公開画像の制約」として利用者に伝える。

---

## 16. 空の要素に付いた罫線は、紙の上に線だけ残る

見積書の備考欄は `border-left: 2px solid` を持つ。備考が空でも要素を
描いていたため、**中身のない縦線だけが印刷される**不具合になった。

装飾（罫線・背景・パディング）を持つ枠は、**中身があるときだけ描く**。
場所を確保したいだけなら、装飾なしのスペーサーを別クラスで置く。

---

## 17. データ検証は「取りこぼし」と「そもそも非公開」を区別する

`validate.cjs` は JAN と入数を必須にしていたが、メーカーによっては
どちらも公開していない（ライフリーは入数もJANも非公開、カミ商事はJANなし）。
必須のままだと、正しいデータなのにエラーが 162 件出て検証が機能しなくなる。

- **規格文字列から読み取れるのに未入力** → エラー（本当の取りこぼし）
- **メーカーが公表していない** → 補足として件数を表示

推測値で埋めるのは絶対にしない。チラシは客先に渡るので、
誤った入数・JANはクレームに直結する。

---

## 18. 商品の並べ方は「メーカー別」より「用途別」が求められる

当初はメーカーごとにページを作ったが、営業の使い方は
「テープ止めを比較して見せる」「パッドを並べて見せる」であり、
**メーカーが混ざってよいので種類で揃えたい**という要望になった。

種類別にすると各カテゴリの件数が4の倍数になりにくい。
1ページ4商品を崩さないために、**カテゴリの切り分け自体を調整する**
（例：カバー2件＋装着パッド2件＝1ページ、5件のシリーズは3件と2件に分ける）。
自動改ページ任せにすると1〜3枚のページができて見栄えが崩れる。

---

## 19. 別アプリに作り込まれた成果物を引き取る

Codex が商品写真144点を発注スキャンアプリ（`taiyo-hacchu-scan`）へ入れて
しまったことがある。作業は Codex のワークツリー
`Documents\Codex\<日付>\<セッション>\work\<リポジトリ>` に残る。

引き取るときは、画像だけでなく**メタデータJSON**（`src/*photo-catalog.json`）
を必ず一緒に見る。画像ファイル名と商品の対応は配列の**添字**で決まっていた
（`001.jpg` = items[0]）ので、JSONを見ずにファイル名だけで移すと
商品と写真がずれる。

---

## 20. 公式サイトの「一覧表」に一次情報が隠れていることがある

ユニ・チャームは商品ページに入数・JANを載せていないが、
「おむつ対応商品一覧」（`/ja/procare/charts.html`）の**病院・施設用欄**に
入数が書かれ、JANは**バーコード画像**として置かれていた。

- 画像のファイル名（`code_cat03_06_hosp.gif`）と商品の対応は
  **連番ではない**（横モレLLが cat03_06、のび〜るフィットMが cat03_04）。
  必ず DOM の出現順で商品名と画像を突き合わせる。
- 画像は 106×28px で1モジュール=1px。劣化していて
  **バーコードとしてはデコードできない**（92〜93モジュールしか残らない）。
  数字部分を拡大して読み、**全件チェックデジットで検証**する。
  読み違えれば9割の確率で不一致になるので、実用上の裏取りになる。

## 21. 施設用と店頭用は入数もJANも別物

同じ「横モレ安心テープ止め M」でも 施設用23枚／店頭用20枚、JANも別。
通販サイトは店頭用が多いので、**そのまま採用すると入数違いの見積になる**。
一次情報がどちらの商品を指しているかを必ず確認する。

自治体の「おむつ給付事業 取り扱い品目単価表」は JAN と入数が併記されていて
一見便利だが、**同じJANが別商品に重複して入力されている**行があった。
公的資料でも二次入力されたものは裏取りに使わない。

## 22. 品番からJANを推定してはいけない

サラヤのJANは `4987696` + 品番5桁 + チェックデジット という規則が
何十件も当てはまるが、**ドライロールだけ `4973512` 始まり**だった。
規則が成り立つように見えても、出典に書かれていない値は入れない。

## 23. 写真は仕様の検算に使える

- ウィル・ステラVHは品番42428（1L）が付いていたが、写真のラベルは5L。
  正しくは42430で、仕切は3,454円ではなく15,397円だった。
- サージカルマスクは「LEVEL2」と登録されていたが、写真の箱は
  「LEVEL1・フリーサイズ・ホワイト」。

**規格が疑わしいときは商品写真を開いて読む。** 価格を当てる前に必ず
容量・レベル・入数を写真と突き合わせる。

---

## 24. 商品パッケージの写真が、いちばん強い一次情報

メーカーが入数を公開していなくても、**パッケージの箱に印字されている**ことが多い。
ライフリーの「病院・施設用専用品」19商品は、公式サイトのどこにも入数がないが、
商品写真の箱に「36枚」「48枚」と大きく刷られていた。

信頼できるかの検算方法：**公式の一覧表に載っている商品で、
写真の枚数と一覧表の枚数が一致するか**を先に確かめる。
全件一致したので、同じ読み取り方を残りにも適用してよいと判断できた。

## 25. 写真の割り当て違いは「箱の表示」で総点検する

ライフリーのサイズ違い5系統すべてで、**SとMの写真が入れ替わっていた**
（Lサイズ以降は正しい）。1商品ずつでは気づきにくく、
商品名つきの一覧シートに並べて、箱に印字されたサイズ・枚数と
突き合わせて初めて分かった。

```python
# 商品名をラベルにして写真を並べる → 箱の表示と目で突き合わせる
```

サイズ・容量・レベルの違う商品を扱うときは、**必ずこの総点検をする**。
同じ確認でカミ商事・第一衛材は全サイズ正しいことも確認できた。

## 26. 再描画するUIの検査は、要素を毎回引き直す

価格シートで「仕入価格を入れても販売価格が出ない」と誤検知した。
実際は入力のたびに `body.innerHTML` を作り直しており、
検査スクリプトが**古いDOMノードの値を読んでいた**だけだった。

```js
// ✗ 参照を使い回す        const el = q('.sell'); ... el.value
// ○ 毎回引き直す          const read = () => q('.sell').value;
```

同じ理由で、一覧から4点選ぶ操作も「毎回先頭の未選択を引き直す」書き方にする。

## 27. 検査で confirm / alert を止め忘れるとタブごと固まる

保存機能の検査で `window.confirm` だけ差し替えて `alert` を残したため、
ダイアログでタブが固まり、その後の `navigate` も 300 秒タイムアウトした。
**操作系を自動で叩く前に confirm と alert の両方を差し替える。**
固まったら新しいタブを開いて続ける。
